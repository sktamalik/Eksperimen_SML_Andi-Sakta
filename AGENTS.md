# AGENTS.md

Repo ini adalah pengerjaan submission **SMSML (Machine Learning Lifecycle)** Dicoding. Lihat `submission.md` untuk ketentuan lengkap 4 kriteria.

## Agent skills

### Issue tracker

Issues live di GitHub Issues via `gh` CLI. See `docs/agents/issue-tracker.md`.

### Domain docs

Single-context layout (satu `CONTEXT.md` + `docs/adr/` di root; belum dibuat — dibuat lazily oleh `/domain-modeling`). See `docs/agents/domain.md`.
